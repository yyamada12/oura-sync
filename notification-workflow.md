# ランニング通知の運用

最終更新：2026年9月19日。日付・時刻はAsia/Tokyo。

## 確定している運用

- 会話・定期実行で編集した関連ファイルは、確認後に都度mainへコミット・pushする（2026年9月19日の本人指示）。親GitHubとSites専用リモートを区別する。詳細はAGENTS.md。

- 個人用ダッシュボードの更新手順は `dashboard-workflow.md`。日次ラン通知・週次見直しに合わせ、計画と実績のスナップショットを更新する。Sitesは本人限定で運用する。

- 毎朝8:10にAIが計画とラン実績・Ouraデータを確認し、その日のメニューを調整してLINEへ送る。
- 現在は既存の会話で続けるheartbeat方式。自動化IDは `automation`。毎回新しい会話で実行する方式は話題に上がったが、まだ切り替えていない。
- 基準計画は `training-plan-2027-02-14.md`。会話で確定した変更は必ずファイルへ反映し、別の会話からも引き継げる状態にする。
- 予定上の休養日・本人が中止と決めた日は、データ取得・日次レポート・LINE送信を行わない。
- ラン予定日に分析の結果として休養へ変更する場合は、その変更を短く知らせる。
- LINE本文は例に示す程度の短い2行（目安80〜140字）。時間帯・距離・所要時間・ペース・体感・重要な注意点に絞る。見出し、挨拶、過去の実績一覧、Ouraの数値、翌日の予定、詳しい分析は付けない。

例：

> 今日の夜は、ゆっくり10km（約58〜65分）。
> 目安は5:45〜6:30/km、会話できる楽さで。最初の2kmはゆっくり、最後も上げず、流しはなし。

## データと判断

- ラン実績：BigQuery `spherical-depth-263101.running.runs`。必要なカラムで直近28日を確認する。
- Oura：Google Sheets `12ck96MZM5bhl5WW7iJWyFb8MSmBJhI2RvwG8_vXN2q4` の `daily_sleep`、`daily_readiness`、`sleep`。睡眠・回復度・心拍・HRVを本人の過去14〜28日の傾向と比較する。
- 同期設定は朝8時台なので、8:10時点で当日データがない場合がある。データ日付と睡眠区間を確認し、未取得の数値や回復状態を推測しない。必要なら「回復データ未反映のため体感優先」と短く添える。
- 好調でも急に負荷を増やさない。疲労・痛みの本人申告を優先し、必要なら短縮や休養を提案する。恒久的な計画変更と当日限りの提案を区別する。

## LINE送信

- 送信スクリプト：`scripts/send_line_plan.py`。認証情報はGit対象外の `secrets/line-notify.json` を参照する。
- `/usr/bin/python3 scripts/send_line_plan.py --key run-plan-YYYY-MM-DD` に本文を標準入力で渡す。日付は日本時間。同日の再試行では同じキーを使い、重複送信を防ぐ。
- 2026年9月19日にテスト送信し、ユーザーがLINEへの到達を確認済み。その後、定期実行のプロンプトもLINE送信を含む形に変更済み。
- APIのHTTP 200は受付成功であり、端末到達の確認ではない。失敗時は送信済みと報告せず、このタスクで簡潔に知らせる。
- LINEからChatGPTアプリの特定のリモート会話を開くリンクは未確認のため、付けていない。
- ローカル実行のため、実行時にMacとアプリが稼働し、必要なデータへのアクセスが可能であることが前提。

実際の自動化設定はアプリが管理する。この文書の編集だけではスケジュールや送信動作は変わらない。設定を変更するときは自動化ツールで変更し、この文書も整合させる。

## 毎週の計画見直し

- 毎週日曜8:10（日本時間）に、翌月曜〜日曜の計画を見直す。毎回新しい会話で実行する独立したスケジュールタスクを使う。
- 自動化ID：`automation-2`。2026年9月19日に作成・有効化。初回予定は9月20日で、9月21〜27日を対象にする。
- 作業場所はこのローカルリポジトリ。日次通知が読めるよう、見直した日別計画を `training-plan-2027-02-14.md` に書き戻す。週の開始日で対象を明示し、既存の同じ週の計画と矛盾させない。
- 天気予報の対象地域：埼玉県川口市（2026年9月19日に本人が指定）。川口市の予報を優先し、広域の予報しか取得できない場合は対象範囲と不確実性を明記する。
- BQのラン実績、Ouraの回復傾向、現在の負荷段階、本人の時間制約、最新の週間天気予報で日別メニューを調整する。日曜朝時点ではその日夜のランは未実施なので、実績に含めない。
- 雨が予想される時間のランは、利用可能な別の時間帯・日に移すことを優先する。高負荷日同士を隣接させず、移動先がなければ短縮・休養も選ぶ。小雨と強雨・雷・強風を区別し、日単位の予報から時間帯の雨を断定しない。
- 翌週後半など予報がない日や不確実な日は、基本案と雨天時の代替案を残す。予報の地域・取得日時・出典を記録する。
- 詳細は新しい実行会話で報告する。週次の長文をLINEへ追加送信する運用は未設定。日次のLINE通知はこれまでどおり短文で運用する。
- 日曜が休養日でも週間見直しは実行する（日次ラン通知の休養日スキップとは別）。日次と同時実行でも当日の日程は書き換えず、翌週のみ更新する。
