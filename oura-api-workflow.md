# Oura API直接参照の運用

2026年9月23日、本人の依頼により日次・週次の回復判断をSheetsからOura APIへ切り替えた。

## 取得方法

依存ライブラリを確認済みのPythonで次を実行する。日付を省略すると日本時間の今日、期間は当日を含む28暦日。

```sh
/Users/01027782/.anyenv/envs/pyenv/versions/3.12.4/bin/python3 /Users/01027782/work/run/scripts/read_oura_recovery.py --days 28
```

- `daily_sleep`、`daily_readiness`、`sleep` をページング込みでGETし、分析に必要な項目だけJSONへ出力する。Sheets/BQへの同期書き込みは行わない。
- `fetched_at` は取得日時、各レコードの `day` と睡眠区間がデータの対象日。昨夜の睡眠が今日分か確認する。主睡眠と昼寝・短い睡眠を区別し、直近14〜28日と比較する。
- 欠測・部分取得失敗は明記する。古いレコードを当日分に扱わず、数値を補作しない。API失敗時は計画とBQ実績から暫定判断し、Sheetsの古いデータへ無言で切り替えない。
- LINEでは必要なら「Ouraの昨夜の睡眠が未取得のため体感優先」と短く書く。ラン記録の取得状況と区別する。

## 認証と同時実行

- Git対象外の `.env` のOAuthクライアント設定と `tokens.json` を既存の `oura_sync.oura_auth` 経由で使用する。キー・トークン・APIエラー本文を出力しない。
- 期限切れなら既存OAuthの通常refreshを行い、ローテーションしたトークンを権限600で原子的に保存する。この通常更新は直接取得に必要な運用として許可済み。ローカルの利用プロセス間は `tokens.lock` で排他する。
- 認証失敗なら再認可が必要と報告する。無人実行でブラウザ認可を開始したり、GASのトークンを取り出して流用しない。
- GASの同期は別トークン系列を使用する。既存の履歴保存用Sheets/BQ同期は維持し、トリガー・認証設定を変更しない。

## ラン実績と定期タスク

- ランは `spherical-depth-263101.running.runs` を継続利用する。Nike Run Clubの全記録がOura APIのworkoutにあるとは限らない。
- 日次 `automation`、週次 `automation-2` はこの方法で回復分析する。実行時刻、日次休養日の無通知、短いLINE本文、週次の新規タスク、川口市の天気確認は従来どおり。
- ダッシュボードは計画とBQラン実績を反映する。Ouraの認証情報や回復データをGitHubやSiteへ追加しない。
